# messaging_app/urls.py
from django.urls import path
from . import views

urlpatterns = [
   # path('inbox/', views.inbox, name='inbox'),
    #path('send/<int:user_id>/', views.send_message, name='send_message'),
    # Add other messaging-related URLs...
]
